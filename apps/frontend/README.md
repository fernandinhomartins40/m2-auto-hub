# Frontend M2

Aplicacao React/Vite da M2, mantida como frontend principal do workspace.

## Comandos

```sh
npm install
npm run dev
npm run build
npm run test
```

Por padrao a aplicacao sobe na porta `3000` e consome a API via `/api`.
